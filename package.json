{
	"scripts": {
		"api": "json-server --watch db.json --port 3000",
		"api:enhanced": "node server.js",
		"cf:dev": "wrangler dev",
		"cf:deploy": "wrangler deploy"
	},
	"dependencies": {
		"json-server": "1.0.0-beta.3"
	},
	"devDependencies": {
		"wrangler": "^3.80.0"
	}
}
